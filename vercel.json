{
  "installCommand": "npm install --prefix client && pip install -r requirements.txt",
  "buildCommand": "npm run build --prefix client && gunicorn --chdir api index:app",
  "outputDirectory": "client/dist",
  "functions": {
    "api/index.py": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
